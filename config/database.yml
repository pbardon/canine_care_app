default: &default
  adapter: postgresql
  encoding: unicode
  username: rails_dev 
  password: ccdevpw123
  pool: 5
  timeout: 5000

development:
  <<: *default
  host: 127.0.0.1
  database: canine_care_development

production:
      <<: *default
      adapter: postgresql
      encoding: unicode
      pool: 5
      database: canine_care_production
      username: <%= ENV['PG_USERNAME'] %>
      password: <%= ENV['PG_PASSWORD'] %>
      host:     <%= ENV['PG_INSTANCE'] %>

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.

test:
  <<: *default
  host: 127.0.0.1
  database: canine_care_test
